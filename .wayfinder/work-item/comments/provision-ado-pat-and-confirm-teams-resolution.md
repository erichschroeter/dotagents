## Resolution

Completed as a byproduct of resolving "Define the list/show query contract (assignee, sprint timeframe, team backlog)" — see that ticket's resolution comment for full detail.

PAT provisioned (Work Items Read & Write scope), authenticated live against `bradycorp1`/`AEPP` via `az devops login`, confirmed real team names: **OS**, **Test Automation**, **THT** (all three match what was assumed while charting). OS's area path (`AEPP\OS`) and current iteration (`AEPP\OS\Sprint 59`) also confirmed live. Session logged out afterward (`az devops logout`); PAT not persisted anywhere in this repo.
